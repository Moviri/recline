<h4>View.D3table</h4>
<p>
This example creates a D3 table. It's a custom table, drawn graphically on the SVG, with the ability of rendering a twin barchart inside any column.<br>
Such a column will then display two percentage values into one single cell. 
</p>